<script>
       1. Create XMLHTTP REQUEST OBJECT
          var xhr = new XMLHttpRequest();
        2. DEFINE A CALLBACK FUNCTION
            xhr.onreadystatechange = function () {
              if ( xhr.readyState === 4){
              
              /* Create a variable that will hold the data from the XHR request */
              /*
              const employees = JSON.parse(xhr.response);
              console.log(emplyees); 
              
          }
        };
        3. OPEN A REQUEST
           xhr.open('GET', 'sidebar.html');
        4. SEND THE REQUEST
          function sendAJAX() {
             xhr.send();
             document.getElementById('load').style.display = "none";
        };
  </script>
  
  
  JSON.parse(); - take a string and tries to convert it into an object 
  JSON.parse method is built-in almost every browser out there - evern Internet Explorer 8. 
